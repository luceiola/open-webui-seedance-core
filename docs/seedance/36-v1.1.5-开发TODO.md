# Seedance v1.1.5 开发 TODO（分组 Key 路由）

发布日期：2026-05-08

## 0. 冻结目标

- [ ] Seedance 只保留一个工具入口，不按 key 分叉 tool/skill。
- [ ] API Key 选择统一在后端按用户组路由。
- [ ] 本版本不做本地计费统计，费用在厂商平台查看。
- [ ] 路由策略对后续 HappyHorse / GPT-Image-2 可复用。
- [ ] 对 LLM 暴露稳定路由错误码，确保可执行的错误提示。

## 1. P0 配置与数据结构

- [ ] 新增 `config/key_routing.json` 样例与加载逻辑。
- [ ] 定义 `providers.<name>.credentials`（alias -> env）结构。
- [ ] 定义 `providers.<name>.bindings`（group_id -> alias）结构。
- [ ] 支持 `strict_single_group` 与 `default_alias` 策略字段。
- [ ] 配置解析失败时提供明确启动日志与错误信息。

## 2. P0 路由解析器

- [ ] 新增统一解析函数：`resolve_provider_credential(provider, user_id, db)`。
- [ ] 读取用户组：复用 OpenWebUI Groups 数据。
- [ ] 命中 0 组时拒绝（默认策略）。
- [ ] 命中多组时拒绝（冲突保护）。
- [ ] 命中单组时返回 `credential_alias + api_key`。
- [ ] `alias` 对应 env 缺失时拒绝并输出可定位日志。

## 3. P0 Seedance 接入

- [ ] 替换 Seedance provider 请求头构造，改为路由解析后的 key。
- [ ] Seedance 任务提交路径写入 `credential_alias`。
- [ ] Seedance 状态刷新路径优先复用任务内 `credential_alias`。
- [ ] 保持现有任务桥接和归档流程不变。

## 3.1 P0 Tool/Skill 错误类型改造

- [ ] 后端定义并冻结 key 路由错误码集合（`KEY_ROUTING_*`）。
- [ ] tool 统一透传错误字段：`ok/status_code/error_code/error_message/request_id`。
- [ ] tool 禁止把路由错误改写为不带错误码的自由文本。
- [ ] skill 增加错误码分支提示模板（管理员处理建议）。
- [ ] 保持 skill 对 `error_code/error_message/request_id` 的原样回传规则。

## 4. P1 任务记录字段

- [ ] 任务记录新增 `credential_alias` 字段。
- [ ] 可选新增 `routing_group_id` 字段。
- [ ] 历史任务兼容读取：字段缺失不影响展示与下载。
- [ ] `/api/v1/tasks` 返回结构向后兼容。

## 5. P1 管理与文档

- [ ] 补充管理员操作说明：建组、加人、配置绑定、重启验证。
- [ ] 补充故障排查说明：无组、组冲突、env 缺失、alias 无效。
- [ ] 在部署文档补充配置变更生效方式（重启/热加载策略）。
- [ ] 补充二开版本规范：`v x.x.x`（大版本）与 `build xxxxx`（开发版本）。

## 5.1 P1 关于页版本展示

- [ ] 在「设置 > 关于」增加二开版本显示项。
- [ ] 前端展示 `v x.x.x` 与 `build xxxxx`。
- [ ] 版本信息由后端配置接口下发，前端不硬编码。
- [ ] 未配置值时显示 `unknown`，避免空白。
- [ ] `build` 改为构建时间戳，格式 `YYYYMMDD_HHmm`。
- [ ] 明确构建时间戳时区口径（建议 `Asia/Shanghai`）。
- [ ] 可选支持 `时间戳+短提交号` 组合显示（便于排障）。
- [ ] 验证 dev 环境可见且样式不破坏原页面布局。

## 6. P1 测试清单

- [ ] 正例：3 组 3 用户分别命中 `k1/k2/k3`。
- [ ] 正例：同一用户多次提交 alias 稳定。
- [ ] 反例：无组用户提交被拒绝。
- [ ] 反例：同用户多组冲突被拒绝。
- [ ] 反例：alias 对应 env 缺失被拒绝。
- [ ] 回归：任务提交、状态刷新、任务页展示、下载不回归。
- [ ] 关于页可正确显示 `v` 与 `build`，并与配置值一致。
- [ ] 关于页 `build` 值为时间戳格式，且符合约定时区。
- [ ] 路由错误场景返回约定 `error_code`（非空、可判定）。
- [ ] skill 对 `NO_GROUP/MULTI_GROUP/ENV_MISSING` 等错误码输出正确动作建议。

## 7. 发布计划

- [ ] 先 dev 发布并完成最小测试清单。
- [ ] 观察 1 天：重点看失败率和路由错误日志。
- [ ] 验证通过后再进入下一版本发布流程。
- [ ] prod 本阶段不自动联动，保持手工受控发布。

## 8. 未来扩展（不纳入 v1.1.5）

- [ ] 同路由框架接入 HappyHorse key 分组。
- [ ] 同路由框架接入 GPT-Image-2 key 分组。
- [ ] 增加管理端可视化配置（替代手改 JSON）。
- [ ] 增加路由生效审计接口（只返回 alias，不返回明文 key）。
