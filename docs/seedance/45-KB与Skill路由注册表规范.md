# 45-KB与Skill路由注册表规范（WebUI框架侧引用）

本文档位置保留在 `docs/seedance`，用于 WebUI 框架开发文档引用。

Skill 开发与路由治理规范已迁移到：

- `templates/docs/45-KB与Skill路由注册表规范.md`

后续约定：

1. Skill 开发文档统一放在 `templates/docs/`。
2. WebUI 框架开发文档继续放在 `docs/seedance/`。
3. 当框架文档涉及 Skill 行为时，引用 `templates/docs/` 对应文档，不再重复维护两套正文。
