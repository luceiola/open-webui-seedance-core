# Seedance v1.1.5.2 开发 TODO（修正版）

发布日期：2026-05-13

## 0. 冻结目标

- [x] 修正项冻结为 6 条（复制、失败原因、自动筛选、时间项、删除筛选、刷新按钮样式）。
- [x] 自动筛选采用 200ms 防抖。
- [x] 失败原因仅在弹窗展示。

## 1. P0 修正项

- [x] 前端：复制按钮改为“clipboard + execCommand fallback”双通道。
- [x] 前端：弹窗新增失败原因区块（仅失败任务显示）。
- [x] 前端：筛选改为选择即应用，移除查询按钮。
- [x] 前端：时间筛选改为 all/24h/7d/30d/90d/180d。
- [x] 前端：移除“包含已删除”筛选与 URL 同步逻辑。
- [x] 前端：刷新按钮移动到筛选同一行并改正方形同高。
- [x] 后端：列表输出前补齐 `error_message/error_code/request_id` 的 raw payload 回填。
- [x] 后端：任务项补充 `archive_error` 输出。

## 2. P1 修正项

- [x] 前端：兼容旧 URL 参数（`custom/start_at/end_at/include_deleted`）并自动回退到新筛选状态。
- [x] 文档：同步 v1.1.5.2 需求/契约/发布检查项。

## 3. 测试与回归

- [x] 为每个 P0 修正项补最小测试（单测/接口测试/前端交互验证之一）。
- [ ] 回归任务主链路（创建、列表筛选、详情浮层、下载）。
- [ ] 回归 key routing 关键错误码（如 `KEY_ROUTING_ENV_MISSING`）。

## 5. 已完成验证

- [x] `pytest backend/open_webui/test/apps/webui/routers/test_v113_unified_tasks.py -q` 通过（17 passed）。
- [x] `npm run build` 通过（存在历史 warning，但无构建失败）。

## 4. 发布步骤（dev -> prod）

- [ ] dev 拉取并 build。
- [ ] dev 验证 6 条修正项全部通过。
- [ ] 确认无回归后再进入 prod 发布窗口。
