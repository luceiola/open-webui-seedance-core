# Seedance v1.1.5.1 开发 TODO（任务页面筛选优化）

发布日期：2026-05-09

## 0. 冻结目标

- [x] 任务页新增“时间 + 用户组”筛选。
- [x] 用户筛选默认值为当前用户。
- [x] 移除模型筛选输入框。
- [x] 浮层展示提示词与生成参数。
- [x] 提示词内资源引用可点击下载。
- [x] 新增“复制提示词和参数”按钮。
- [x] URL 持久化筛选状态。
- [x] 自动刷新仅处理 `PENDING/RUNNING`。
- [x] 列表显示“当前结果数 / 总数”。

## 1. P0 后端接口

- [x] `GET /api/v1/tasks` 增加 `group_id/start_at/end_at` 参数解析。
- [x] 任务列表过滤逻辑增加“按用户组”筛选。
- [x] 任务列表过滤逻辑增加“按创建时间区间”筛选。
- [x] 时间过滤使用 `created_at`，支持闭区间处理。
- [x] 增加任务页组列表接口（已登录用户可访问，返回最小字段）。
- [x] 组列表接口返回结构字段冻结：`group_id/group_name`。

## 2. P0 任务记录标准字段

- [x] `task_record` 写入 `prompt_text`。
- [x] `task_record` 写入 `generation_params`（对象）。
- [x] `task_record` 写入 `prompt_resources`（`[{name,url}]`）。
- [x] 新字段在 `bridge/upsert` 路径可更新。
- [x] 老记录缺失字段时保持兼容读取。

## 3. P0 前端筛选区与查询参数

- [x] 任务页移除模型输入框及其查询参数。
- [x] 新增用户组下拉筛选。
- [x] 新增时间筛选（24h/7d/30d/自定义）。
- [x] 自定义时间使用 `datetime-local` 起止控件。
- [x] 默认 `selectedUserId = $user.id`。
- [x] URL Query 与筛选状态双向同步。
- [x] 首次加载时优先从 URL 还原筛选。
- [x] 查询按钮与刷新按钮保持当前筛选语义一致。

## 4. P0 浮层信息增强

- [x] 浮层新增“提示词”区块。
- [x] 浮层新增“生成参数”区块。
- [x] 提示词资源引用渲染成超链接（下载导向）。
- [x] 增加“复制提示词和参数”按钮。
- [x] 复制内容格式：仅可读文本。
- [x] 无字段时展示“暂无”占位，不抛异常。

## 5. P1 刷新与展示优化

- [x] 自动刷新逻辑仅轮询 `PENDING/RUNNING` 项。
- [x] 手动刷新保留完整刷新能力。
- [x] 列表顶部展示“当前结果数 / 总数”。
- [x] 对大列表场景确认性能不回退。

## 6. P1 测试清单

- [x] 单元测试：`group_id/start_at/end_at` 过滤正确。
- [x] 单元测试：时间边界（仅 start、仅 end、start>end）行为明确。
- [x] 单元测试：组列表接口权限（普通用户可读全部组）。
- [x] 单元测试：老任务缺失 `prompt_*` 字段兼容通过。
- [x] 前端联调：默认用户筛选为自己。
- [x] 前端联调：URL 持久化可回显。
- [x] 前端联调：浮层复制内容正确。
- [x] 前端联调：提示词资源链接可点击下载。
- [x] 回归：单用户筛选、全部用户筛选均稳定。

## 7. 发布步骤（dev -> prod）

- [ ] 合并后先在 dev 拉取并 build。
- [ ] 验证任务页四类场景：
- [ ] 我的任务
- [ ] 全部用户
- [ ] 指定用户组
- [ ] 指定时间范围
- [ ] 验证浮层复制与资源链接。
- [ ] 验证 URL 刷新不丢状态。
- [ ] dev 验证通过后再做 prod 发布。
