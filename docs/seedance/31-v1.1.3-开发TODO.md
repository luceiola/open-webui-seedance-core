# Seedance v1.1.3 开发 TODO（统一任务中心）

发布日期：2026-05-07  
修订日期：2026-05-08

## 0. 版本冻结项

- [ ] 冻结目标：统一任务模型 + Adapter 架构 + Seedance/HappyHorse 接入。
- [ ] 冻结接口：`/api/v1/tasks/*` 作为前端唯一任务数据源。
- [ ] 冻结规则：全员可见、删除权限 `owner_or_admin`、自动归档、软删除保留期 7 天。
- [ ] 冻结字段级契约：完成并评审 `32-v1.1.3-接口契约.md`。

## 1. P0 数据层（统一任务存储）

- [ ] 新增 `tasks` 存储（表或等价持久层）：
  - [ ] `id/provider/provider_task_id/tool_name/skill_name`
  - [ ] `user_id/user_name/chat_id/model`
  - [ ] `status/progress/error_code/error_message`
  - [ ] `deleted_at/deleted_by/created_at/updated_at/finished_at`
- [ ] 新增 `task_artifacts` 存储：
  - [ ] `task_id/artifact_type/storage_backend/storage_path`
  - [ ] `mime_type/bytes/width/height/duration_ms`
  - [ ] `preview_ready/download_ready`
- [ ] 新增索引（最小）：
  - [ ] `tasks(created_at desc)`
  - [ ] `tasks(user_id, created_at desc)`
  - [ ] `tasks(status, created_at desc)`
  - [ ] `task_artifacts(task_id)`
- [ ] 冻结唯一约束：
  - [ ] `UNIQUE(provider, provider_task_id)`

## 2. P0 服务层（统一任务服务）

- [ ] 抽象 `TaskService`：
  - [ ] `create_task`
  - [ ] `update_task_status`
  - [ ] `attach_artifact`
  - [ ] `soft_delete`
  - [ ] `list_tasks`
- [ ] 统一状态规范：
  - [ ] `PENDING/RUNNING/SUCCEEDED/FAILED/CANCELED`
  - [ ] 归档 `NOT_REQUIRED/PENDING/RUNNING/SUCCEEDED/FAILED`
- [ ] 统一权限校验函数：
  - [ ] 读：登录用户
  - [ ] 删/重试/取消：创建者或管理员

## 3. P0 Adapter 框架

- [ ] 新建 provider adapter 抽象接口：
  - [ ] `submit(input)`
  - [ ] `query(task_id)`
  - [ ] `cancel(task_id)`
  - [ ] `extract_artifacts(response)`
  - [ ] `validate_input(input)`
- [ ] 实现 `seedance_adapter` 对接统一任务服务。
- [ ] 实现 `happyhorse_adapter` 对接统一任务服务。
- [ ] 加入 provider 错误规范化：
  - [ ] 标准错误码映射
  - [ ] 标准错误消息映射

## 4. P1 API 层（统一任务接口）

- [ ] 提供并联调：
  - [ ] `GET /api/v1/tasks`
  - [ ] `GET /api/v1/tasks/providers`
  - [ ] `GET /api/v1/tasks/users`
  - [ ] `GET /api/v1/tasks/{task_id}`
  - [ ] `GET /api/v1/tasks/{task_id}/preview`
  - [ ] `GET /api/v1/tasks/{task_id}/download`
  - [ ] `DELETE /api/v1/tasks/{task_id}`
  - [ ] `POST /api/v1/tasks/{task_id}/archive/retry`
  - [ ] `POST /api/v1/tasks/{task_id}/cancel`
- [ ] 列表返回统一为：
  - [ ] `items`
  - [ ] `total`
  - [ ] `offset`
  - [ ] `limit`
- [ ] 支持筛选参数：
  - [ ] `user_id/provider/skill_name/tool_name/status/model/include_deleted`

## 5. P1 归档与下载链路

- [ ] 任务成功后自动归档（worker/poller）。
- [ ] 平台下载统一走归档文件流。
- [ ] 缩略图生成（视频首帧）并写入 artifact。
- [ ] 归档失败重试（最大次数可配置）。
- [ ] 上游 URL 过期时，已归档任务仍可下载。

## 6. P1 前端接入

- [ ] 任务页 API 从 `material-packages/tasks` 切换到 `/api/v1/tasks/*`。
- [ ] 保持现有 UI 行为不变：
  - [ ] 网格 3-8 列
  - [ ] Cell 比例 3:4
  - [ ] 卡片下载图标
  - [ ] 浮层下载图标
  - [ ] 浮层删除图标（有权限）
- [ ] 新增 provider 筛选项（可选：先隐藏开关）。

## 7. P1 迁移与兼容

- [ ] Seedance 双写开关（旧 JSON + 新存储）。
- [ ] 历史任务回填脚本：
  - [ ] 输入：`material_packages/*/tasks/*.json`
  - [ ] 输出：`tasks + task_artifacts`
- [ ] 回填校验脚本：
  - [ ] 数量校验
  - [ ] 抽样详情校验
- [ ] 观察期后关闭旧读取路径。
- [ ] 切流门槛校验：
  - [ ] 双写稳定 24h
  - [ ] 双写失败率 < 0.1%
  - [ ] 抽样字段一致率 >= 99.9%
  - [ ] 回填覆盖率 >= 99.9%
  - [ ] `GET /api/v1/tasks` P95 <= 500ms
  - [ ] 已归档任务下载成功率 >= 99.0%
- [ ] 回滚触发监控：
  - [ ] 5xx 连续 10 分钟 > 1.0%
  - [ ] 双写失败率连续 10 分钟 > 1.0%
  - [ ] 下载成功率连续 10 分钟 < 97.0%
  - [ ] 发现高危越权 case

## 8. P2 可观测与治理

- [ ] 指标：
  - [ ] 任务提交成功率
  - [ ] provider 查询成功率
  - [ ] 归档成功率
  - [ ] 下载成功率
- [ ] 存储治理参数落地：
  - [ ] `TASK_ARCHIVE_RETENTION_DAYS=90`
  - [ ] `TASK_ARCHIVE_MAX_FILE_MB=2048`
  - [ ] `TASK_ARCHIVE_USER_QUOTA_GB=200`
  - [ ] 磁盘水位 70/85/90% 告警与保护策略
- [ ] 审计日志：
  - [ ] 删除行为
  - [ ] 归档重试行为
  - [ ] 权限拒绝行为
- [ ] 清理策略：
  - [ ] 软删除超期清理
  - [ ] 孤儿产物清理

## 9. 测试与验收清单

- [ ] Seedance 正常提交/完成/下载全链路通过。
- [ ] HappyHorse 正常提交/完成/下载全链路通过。
- [ ] 两类任务在同一面板可见并可按用户筛选。
- [ ] 非创建者不可删除，管理员可删除。
- [ ] 上游 URL 过期后，归档任务仍可下载。
- [ ] 回填历史任务可检索、可预览（有产物时）、可下载（归档完成时）。

## 10. 发布计划（建议）

- [ ] Day 1：数据层 + 任务服务 + adapter 抽象。
- [ ] Day 2：Seedance adapter 接入 + 统一 API 首版。
- [ ] Day 3：HappyHorse adapter 接入 + 归档链路补齐。
- [ ] Day 4：前端切换 + 回填脚本 + 联调。
- [ ] Day 5：灰度验证 + 生产发布 + 监控观察。
