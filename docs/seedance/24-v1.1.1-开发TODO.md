# Seedance v1.1.1 开发 TODO（发布隔离）

发布日期：2026-05-07

## 0. 目标与约束

- [x] 版本目标冻结：`v1.1.1` 只做发布隔离，不引入 v1.2 功能。
- [x] 前置顺序冻结：`v1.1` -> `v1.1.1` -> `v1.1.2` -> `v1.2`。
- [ ] 变更窗口冻结：v1.1.1 期间禁止直接在 prod 实例做功能试验。

## 1. P0 本地改造（代码与配置隔离）

- [ ] 准备独立目录：
  - [ ] `<repo-dev>` 用于日常开发与联调。
  - [ ] `<repo-prod>` 用于发布与回滚，不在该目录开发。
- [ ] 在 `<repo-dev>` 保持现有 `8802` 启动脚本能力可用。
- [ ] 在 `<repo-prod>` 保持现有 `8801` 启动脚本能力可用。
- [ ] 配置隔离：
  - [ ] `config/ark.env`（prod）与 `config/ark.dev.env`（dev）均存在且字段完整。
  - [ ] 生产与开发 `TOS_PREFIX` 区分（建议 `material-packages-prod` / `material-packages-dev`）。
  - [ ] 若启用媒体专用前缀，同步区分 `MEDIA_ASSET_TOS_PREFIX`。
- [ ] 数据隔离核验：
  - [ ] prod 默认 `DATA_DIR=.data-prod`。
  - [ ] dev 默认 `DATA_DIR=.data-dev`。

## 2. P0 服务器改造（systemd 双实例）

- [ ] 新增/调整 systemd 服务：
  - [ ] `openwebui-seedance-prod.service`（端口 `8801`）
  - [ ] `openwebui-seedance-dev.service`（端口 `8802`）
- [ ] 为两个服务分别指定：
  - [ ] 独立 `WorkingDirectory`（prod/dev 分离）
  - [ ] 独立 `EnvironmentFile`（ark.env / ark.dev.env）
  - [ ] 独立 `DATA_DIR`（.data-prod / .data-dev）
- [ ] 服务启停验证：
  - [ ] 单独重启 dev 不影响 prod。
  - [ ] 单独重启 prod 不影响 dev。

## 3. P1 发布与回滚流程脚本化

- [x] 发布流程脚本已落地：`scripts/seedance/deploy_prod_ref.sh`。
- [x] 开发部署脚本已落地：`scripts/seedance/deploy_dev.sh`。
- [ ] 发布流程固定：
  - [ ] 仅从 `<repo-dev>` 选定 release commit/tag。
  - [ ] 在 `<repo-prod>` 执行 `deploy_prod_ref.sh --ref <tag/commit>`。
  - [ ] 重启 prod 并做健康检查。
- [ ] 回滚流程固定：
  - [ ] 保留上一个稳定 tag（例如 `release/v1.1.0`）。
  - [ ] 异常时执行 `deploy_prod_ref.sh --ref <previous-tag>` + 重启 prod + 验证。
- [ ] 形成统一发布记录模板：
  - [ ] 版本号
  - [ ] commit/tag
  - [ ] 发布时间
  - [ ] 操作人
  - [ ] 验证结果

## 4. P1 验收项（必须全绿）

- [ ] 场景 A：dev 目录修改代码并重启 `8802`，`8801` 行为不变。
- [ ] 场景 B：只重启 `8801`（不更新 prod 目录代码），功能不变化。
- [ ] 场景 C：prod 目录切到新 tag 并重启 `8801`，新版本生效。
- [ ] 场景 D：回滚到上一个 tag 并重启 `8801`，旧版本恢复。
- [ ] 场景 E：prod/dev 的素材写入云端前缀可区分，不混用。

## 5. P2 文档与交接

- [x] 更新运行手册，明确双目录 + 双服务启动方式。
- [x] 更新部署手册，明确“发布”与“开发联调”命令边界。
- [x] 在 `v1.1.2` 与 `v1.2` 文档中明确引用 v1.1.1 作为发布前置条件。

## 6. DoD（完成定义）

- [ ] prod/dev 完成“代码 + 配置 + 数据 + 服务 + 流程”五层隔离。
- [ ] 发布与回滚各完成一次演练并留痕。
- [ ] `v1.1.2` / `v1.2` 开发明确只在 dev 进行，prod 仅通过发布动作更新。
